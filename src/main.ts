/**
 * QuickBasicEmulator_Web — entry point
 *
 * v0.0.1-Gates: skeleton placeholder.
 * v0.0.3-Davidoff: QBJS-fork import + GW-BASIC dialect mode.
 */

import version from '../version.json';

console.log(`QuickBasicEmulator_Web v${version.version}-${version.codename}`);
console.log(`Released: ${version.released}`);
console.log(`Status: ${version.type}`);
